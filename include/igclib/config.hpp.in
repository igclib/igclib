#pragma once

#include <string>

const std::string HEADER =
    "${PROJECT_NAME} v${PROJECT_VERSION}\nusage : ${PROJECT_NAME} "
    "[xc] args [options]\n\nxc\n\t--flight "
    "<path/to/igc/file>\n\t--airspace <path/to/openair/file>\n\noptions : "
    "\n";
